apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: api
  namespace: cohesiv
  annotations:
    kubernetes.io/ingress.class: "nginx"
    ingress.kubernetes.io/ssl-redirect: "true"
    kubernetes.io/tls-acme: "true"
spec:
  tls:
  - hosts:
    - stage.cohesiv.io
    secretName: cohesiv-tls
  rules:
  - host: "stage.cohesiv.io"
    http:
      paths:
      - path: /api
        backend:
          serviceName: api
          servicePort: 8080