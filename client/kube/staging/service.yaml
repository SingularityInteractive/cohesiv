apiVersion: v1
kind: Service
metadata:
  namespace: cohesiv
  name: cohesiv
  labels:
    application: cohesiv
spec:
  type: NodePort
  selector:
    app: cohesiv
  ports:
  - protocol: TCP
    port: 80
    targetPort: 4040
